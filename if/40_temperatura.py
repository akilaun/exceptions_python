temperatura = int(input("Digite a temperatura do dia: "))

if temperatura > 30:
    print(f"Está calor, temperatura = {temperatura}")
    if temperatura > 35:
        print(f"Nesse calor até elefante na bunda sua!, temperatura = {temperatura}")
        
if temperatura < 20:
    print(f"Está frio, temperatura = {temperatura}")

